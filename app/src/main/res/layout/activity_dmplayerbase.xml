<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="@bool/fitsSystemWindows">

    <!-- The toolbar and main content view -->
    <include
        android:id="@+id/include_sliding_panel"
        layout="@layout/include_slidingup_panellayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <!-- The navigation drawer -->

    <fragment
        android:id="@+id/fragment_navigation_drawer"
        android:name="kz.qobyzbook.fragments.FragmentDrawer"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:layout="@layout/fr_nav_drawer"
        tools:layout="@layout/fr_nav_drawer" />
</android.support.v4.widget.DrawerLayout>